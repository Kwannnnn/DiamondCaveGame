class Run {
    constructor(id, totalScore, time, playerUsernames) {
        this.id = id
        this.totalScore = totalScore
        this.time = time
        this.playerUsernames = playerUsernames
    }
}

module.exports = Run;